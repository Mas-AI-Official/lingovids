export default function Layout({ children }) {
  return (
    <html lang="en">
      <head>
        <title>LingoVids</title>
      </head>
      <body>
        {children}
      </body>
    </html>
  );
}
